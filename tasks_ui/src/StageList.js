
const StageList = () => {
  
  return (
    // <div className="flex justify-between">
    //   {stages.map((stage) => (
    //     <StageItem key={stage.id} stage={stage} stages={stages} />
    //   ))}
    // </div>
    <div></div>
  );
};

export default StageList;
